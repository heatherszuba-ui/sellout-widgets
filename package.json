{
  "name": "sellout-widgets",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable sell-out projection widgets for The Micro's Notion dashboards",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "fetch": "node scripts/fetch-projections.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  }
}
